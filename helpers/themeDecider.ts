export const themeDecider = (hostname: string) => {
  //Search in the database for the corresponding theme based on the hostname
  //This will include a memory cache to relieve the database

  return "ONE";
};
